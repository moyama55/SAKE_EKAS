<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>投稿編集</h1>
    	<%= render 'layouts/errors', obj: @alchool%>
  		<%= render 'form', alchool: @alchool %>
  		<%= link_to 'Show', alchool_path(@alchool), class: "alchool_#{@alchool.id}" %> |
  		<%= link_to 'Back', alchools_path, class: "back" %>
  </div>
</div>